{
  "name": "lore-md",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "format": "oxfmt --write package.json pnpm-workspace.yaml tsconfig.json wrangler.toml src test README.md docs",
    "format:check": "oxfmt --check package.json pnpm-workspace.yaml tsconfig.json wrangler.toml src test README.md docs",
    "lint": "pnpm format:check && pnpm typecheck && oxlint --deny-warnings --type-aware --tsconfig tsconfig.json src test",
    "check": "pnpm lint && pnpm test"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260830.1",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "oxlint-tsgolint": "^7.0.2001",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.127.1"
  },
  "engines": {
    "node": ">=22.13"
  },
  "packageManager": "pnpm@11.24.0"
}
